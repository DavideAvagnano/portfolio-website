// `tailwindcss-motion` non fornisce dichiarazioni di tipo. Usato come plugin
// in `tailwind.config.ts` (Tailwind v3). Questo shim verrà rimosso in Fase 3
// con il passaggio a Tailwind v4 (config CSS-first, niente più config JS).
declare module "tailwindcss-motion"
